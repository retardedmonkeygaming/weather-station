# =============================================================================
# Raspberry Pi Weather Station — Python 3.9+ requirements
# Every line maps to a real import in this codebase; nothing speculative.
# =============================================================================

# --- Web stack (dashboard, API, server) ---
fastapi>=0.110,<1.0            # web/routes.py — HTTP API + UI routes
uvicorn[standard]>=0.29,<1.0   # web/routes.py run_web_server() — ASGI server
python-multipart>=0.0.9,<1.0   # web/routes.py — Form(...) endpoints
jinja2>=3.1,<4.0               # FastAPI template plumbing (default_html)

# --- Outbound HTTP + persistence ---
aiohttp>=3.9,<4.0              # services/weather_api.py — Open-Meteo fetches
aiosqlite>=0.20,<1.0           # database.py — WAL-backed SQLite layer

# --- Integrations (optional at runtime; degrade cleanly if unconfigured) ---
discord.py>=2.3,<3.0           # services/discord_bot.py — slash commands, TTS
paho-mqtt>=1.6,<2.0            # services/mqtt_client.py — broker publishing

# --- Raspberry Pi hardware (imported only when running on a Pi) ---
adafruit-circuitpython-charlcd>=4.6   # hardware/lcd_driver.py — 1602A
adafruit-circuitpython-dht>=4.0       # hardware/sensors.py — DHT11
adafruit-blinka>=8.0                  # board/digitalio backends for the above
gpiozero>=2.0                         # hardware/sensors.py — button & buzzer

# --- Optional extras (not required to run) ---
# pyttsx3>=2.90                 # Voice Mode fallback when espeak isn't installed
                               #   (espeak via `apt install espeak` is preferred)
# pytest / pytest-asyncio       # test runner, if you add a test suite

# Frontend note: Tailwind CSS + Chart.js + HTMX load from CDNs in
# web/templates/*.html — no pip package needed for them.
