# ==============================================================================
# SkyCast Weather Station - Requirements
# Compatible with Python 3.9+
# ==============================================================================

# Core Web Framework & Async Stack
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
websockets>=13.0.0,<17.0     # <-- Bumped to fix google-genai conflict
jinja2>=3.1.3,<4.0.0
python-multipart>=0.0.9,<1.0.0

# Hardware & GPIO (Raspberry Pi Setup)
# Note: RPi.GPIO and spidev require Raspberry Pi hardware or desktop mock setups
gpiozero>=2.0,<3.0.0
RPi.GPIO>=0.7.1,<1.0.0; sys_platform == 'linux' and platform_machine == 'armv7l' or platform_machine == 'aarch64'
spidev>=3.6,<4.0.0; sys_platform == 'linux' and platform_machine == 'armv7l' or platform_machine == 'aarch64'
smbus2>=0.5.0,<1.0.0
Pillow>=10.2.0,<11.0.0

# Adafruit CircuitPython Drivers
adafruit-blinka>=8.0.0,<9.0.0
adafruit-circuitpython-ssd1306>=2.11.0,<3.0.0
adafruit-circuitpython-dht>=4.0.0,<5.0.0
adafruit-circuitpython-charlcd>=3.5.7,<4.0.0

# External APIs, Networking & Integrations
aiohttp>=3.9.1,<4.0.0
requests>=2.31.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
paho-mqtt>=1.6.0,<2.0.0

# Discord Bot Integration (Optional — disables itself if token is missing)
discord.py>=2.3.2,<3.0.0

# Configuration, Database & Utilities
pydantic>=2.12.5,<3.0.0      # <-- Bumped to fix google-genai conflict
pydantic-settings>=2.1.0,<3.0.0
aiosqlite>=0.20.0,<1.0.0
asyncio>=3.4.3
typing-extensions>=4.14.0,<5.0.0 # <-- Bumped to fix google-genai conflict

# Optional: Development & Testing (Uncomment if needed)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.12.0
# ruff>=0.1.0
